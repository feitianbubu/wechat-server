# 数据库配置
# 设置此项将使用 MySQL 而非 SQLite
SQL_DSN=root:123456@tcp(localhost:3306)/wechat_server?charset=utf8mb4&parseTime=True&loc=Local

# Redis 配置 (可选)
REDIS_ADDR=127.0.0.1:6379
REDIS_PASSWORD=

# 会话密钥 (可选)
SESSION_SECRET=your-secret-key

# 其他配置
PORT=3000
GIN_MODE=release